Changes Made:

    customize.c:
            direct.c
                Added the ifdef statement for LINUX
                Added LINUX in the if statement as an || option on line 54
            // vtree.c
                // For all NAME functions, changed ((x).d_name) -> ((x)->d_name)


    direct.c:
        Moved opendir method in the ifdef for SYS_III
        Changed dp -> dirp in the readdir() method

    hash.h:
        Added:
            int h_enter(dev_t dev, ino_t ino)
            void h_stats()
        Created an array: memory_array and variable: positions

    hash.c:
        Change return type to void for the 3 functions - void char *malloc(), void char *realloc(), void char *calloc()
        h_enter return type is int
        Incremented positions everytime calloc, malloc, or realloc is called and added to the position in the memory_array

    vtree.c:
        In RD_List pointer, READ entry -> READ *entry
        Commented READ *readdir() and OPEN *opendir()
        170:7 : tmp_entry -> *tmp_entry
        included <stdlib.h>
        341, 378, 406: &tmp_RD->entry -> tmp_RD->entry
        292: (NAME(*file), "..") -> (NAME(file), "..")
        Renamed dup -> duplicate
        down method has return type void
        176: READ *tmp_entry -> READ tmp_entry
        Added on the top:
            static int chk_4_dir(char *path)
            static void get_data(char *path, int cont)
            static int is_directory(char *path)
        Changed the malloc(struct RD_List *) -> malloc(struct RD_List )
        Made head and tail equal to null
        Added free:
            Line 419, 504
        Cleared the memory_array at the end of vtree_main()
        Changed 714: printf("Total inodes: %d\n", inodes) -> printf("Total inodes: %d\n", total_inodes);
        Changes getopt to getopt_long